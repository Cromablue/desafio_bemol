import 'package:flutter/material.dart';

main(){
  runApp(Container(
    child: Center(
      child: Text(
        'Bem vindo, ao seu app de consulta de cotações', textDirection: TextDirection.ltr,
        style: TextStyle(color: Colors.white, fontSize: 60.0),
      ),
    )
  ));
}